# AlphaBuddy（题目 13：投资 X Buddy）架构蓝图与执行清单

> 用途：24 小时冲刺执行蓝图，可直接作为项目 AGENTS.md / README 底稿。
> 架构来源：产品形态参考财搭子（supervisor 多智能体 + 上下文内多产物）；
> Harness 层基于 LangGraph（fork deer-flow），合规层参考 Kimi 金融方案五条。

---

## 0. 产品定义

- **一句话**：面向投资研究者的透明 Agent 工作台——给一个研究目标，它出计划、调真实金融数据、全程可见可控，产出带证据和待核实事项的研究产物。
- **用户**：个人投资研究者（非小白股民，与财搭子区隔）。
- **非目标**：不做买卖建议/涨跌预测/托管择时（合规红线）；不做实时推送；不做多用户与组织治理（WorkBuddy 的 B 端能力砍掉）；不做账户体系（记忆用本地 Store）。
- **对标叙事（写进 README）**：机构方案（腾讯 WorkBuddy 金融版、Kimi 金融方案）把"数据 MCP 化 + 技能封装 + 多 Agent + 审批 + 可审计"卖给券商；本产品验证同一架构在个人研究者场景可落地，且把 harness 内部状态全部透明化。

---

## 1. 总体骨架图

```
┌─────────────────────────── 浏览器（Next.js 单页）───────────────────────────┐
│ 左栏              │ 中栏（步骤流）                  │ 右栏（产物面板）        │
│ · 线程列表        │ · 计划卡（可编辑 / 批准）        │ · 研究报告（Markdown）  │
│   (thread)        │ · 工具调用卡（入参/出参可展开）   │ · 证据清单              │
│ · 新研究目标输入  │ · 审批卡（interrupt 等待态）     │ · 待核实事项            │
│ · 能力页（工具表）│ · 压缩/恢复/失败 事件标记        │ · 导出 .md              │
│ 底部成本条：本任务 token 消耗 / 耗时 / 步数 / 预算余量                        │
└──────────────────────────────────┬───────────────────────────────────────┘
                                   │ SSE 流式（LangGraph state events）
┌──────────────────────────────────▼───────────────────────────────────────┐
│                        后端（Python FastAPI + LangGraph）                  │
│                                                                            │
│  ┌─ Agent 层（fork deer-flow 改造）──────────────────────────────────┐    │
│  │  Supervisor「主研」：意图识别 → 选择技能 → 生成计划                 │    │
│  │      │                                                            │    │
│  │      ▼                                                            │    │
│  │  Planner：产出研究计划（步骤列表，每步绑定工具+字段需求）            │    │
│  │      │                                                            │    │
│  │      ▼  ◄── interrupt：人工审批（批准 / 编辑后再批准）              │    │
│  │  Researcher：按计划循环调用工具，结果回写计划状态                   │    │
│  │      │  冲突检测：同字段双源不一致 → 标记矛盾点，不强行统一          │    │
│  │      ▼                                                            │    │
│  │  Reporter：按技能模板生成产物（证据引用 + 待核实事项 + 失效条件）     │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                            │
│  ┌─ 技能层 Skills（= Kimi"专业技能"的个人版，预置 3 张任务卡）─────────┐    │
│  │  ① 命题验证：验证一句投资说法（复用寒武纪已验证字段表）              │    │
│  │  ② 业绩点评：读最新财报 → 增长/盈利/费用/减值四段点评               │    │
│  │  ③ 持仓早报：对自选标的跑例行扫描（演示定时任务 + 记忆）             │    │
│  │  每张卡 = prompt 模板 + 工具白名单 + 固定产物格式                    │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                            │
│  ┌─ 工具注册表 ToolRegistry ─────────────────────────────────────────┐    │
│  │  统一返回结构：{ data, source, as_of, unit, caliber }              │    │
│  │  每次调用写审计日志（授权检查落实到每次调用 —— Kimi 合规第 3 条）     │    │
│  │  · fuyao_quote_snapshot    扶摇：行情快照                          │    │
│  │  · fuyao_valuation         扶摇：估值                              │    │
│  │  · ifind_fin_indicator     iFinD MCP：财务指标（复用③字段表）       │    │
│  │  · ifind_fin_statement     iFinD MCP：三大报表                     │    │
│  │  · ifind_announcement      iFinD MCP：公告列表 + PDF 链接          │    │
│  │  · ifind_news（可选）      iFinD MCP：新闻/行业                     │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                            │
│  ┌─ Harness 层（LangGraph 内置能力，禁止自研）───────────────────────┐    │
│  │  · Checkpointer（SQLite）：每步落库 → 刷新/崩溃后断点恢复           │    │
│  │  · interrupt()：审批点（计划确认）+ 敏感动作确认                    │    │
│  │  · Store：长期记忆（研究过的标的、历史结论、用户偏好）               │    │
│  │  · recursion_limit + 单工具 10s 超时 + token 预算：停止规则        │    │
│  │  · summarization middleware：上下文压缩，压缩事件上报 UI           │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                            │
│  ┌─ 合规网关（Kimi 五条的个人版）────────────────────────────────────┐    │
│  │  · 输入拦截：涨跌预测/买卖建议类输入 → 拒绝并引导改写为研究目标      │    │
│  │  · 输出核验：产物中的数字必须能回指某次工具返回的字段值              │    │
│  │  · 产物强制含「待核实事项」章节，不直接形成业务结论                  │    │
│  │  · 审计：任务 / 工具调用 / 数据来源 记录关联保存，全程可查          │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│                                                                            │
│  ┌─ 可观测：Langfuse ───────────────────────────────────────────────┐    │
│  │  每次 LLM/工具调用的 token、时延、轨迹 → 汇总给前端成本条           │    │
│  └──────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────┘
```

### 与财搭子架构的对应关系（README"产品选择"素材）

| 财搭子 | 本产品 | 取舍原因 |
|---|---|---|
| 大发（主智能体） | Supervisor「主研」 | 同款 supervisor 模式 |
| 31 个专家智能体 | 3 张技能卡 + 6 个工具 | "31 个 Agent" 实为 prompt 人设 + 工具子集；24h 砍成技能化 |
| 五维分析 + 辩论 | 双源冲突时标记矛盾点 | 辩论机制过重，保留"冲突可见"这个核心价值 |
| 镜像模拟验证 + 复盘 | 产物含失效条件 + 待核实事项 | 验证闭环简化为"什么变化会推翻结论" |
| 24h 扫描 + 推送 | 持仓早报定时任务（P1） | 同一事件驱动机制，去掉推送通道 |
| 真实操作由你确认 | interrupt 审批 | 同一设计原则，也是题目要求 |
| 上下文内多产物 | 右栏产物面板 | 直接对应 |

---

## 2. 评分点 → 实现 → 演示动作对照

| 题目名词 | 实现位置 | 演示动作 |
|---|---|---|
| 能力/工具注册 | ToolRegistry + 前端能力页 | 打开能力页展示工具 schema |
| 规划任务 | Supervisor + Planner + 技能卡 | 计划卡逐步变绿 |
| 调用金融工具 | Researcher + 6 工具 | 工具卡展开看入参/出参 |
| 管理上下文 | summarization middleware | 长任务出现"已压缩"事件标记 |
| 长期状态 | Store | 新会话追问上次结论 |
| 可验证成果 | 产物含证据引用，数字可点回工具返回 | 点报告数字 → 弹出原始字段 |
| 请求用户确认 | interrupt | 编辑一步计划再批准 |
| 检查点与恢复 | SQLite Checkpointer | **任务中途刷新页面，断点续跑** |
| 权限/审批 | 合规网关 + interrupt | "该不该买"被拦截 |
| 停止规则 | 步数/超时/token 预算 | UI 显示预算余量，超限显式停止 |
| 成本/时延 | Langfuse + 成本条 | 任务结束展示总 token/耗时 |
| 可观测性 | Langfuse trace + 审计日志 | 展示一次任务的完整调用链 |
| 降级与恢复 | 工具失败标红继续；LLM 失败重试 | 拔掉 iFinD 配置演示 |
| 线程/任务/证据/产物/动态 UI | 三栏布局 | 整体走一遍即是演示 |

---

## 3. TODO List（24 小时，含验收标准）

### Phase 0：地基（0–1h）——不通就换方案，别硬扛

- [ ] **T0.1** 验证 iFinD MCP 能调通（`https://mcp.51ifind.com/?syncCookieTimes=1`，注意登录态）→ 验收：拉到寒武纪营收增速
- [ ] **T0.2** 读完扶摇文档（https://fuyao.aicubes.cn/docs/），调通行情快照 → 验收：拿到一只股票的实时快照
- [ ] **T0.3** fork deer-flow，原版跑通一次研究任务 → 验收：本地能出报告
- [ ] **T0.4** 环境变量与 .gitignore 就位（API Key 不进仓库，题目明文要求）

### Phase 1：工具层（1–5h）

- [ ] **T1.1** 定义统一工具返回结构 `{ data, source, as_of, unit, caliber }`（四要素焊死在工具层）
- [ ] **T1.2** 封装 4 个核心工具：fuyao_quote_snapshot / fuyao_valuation / ifind_fin_indicator / ifind_fin_statement（字段名直接搬题目③方案里实测过的映射表）
- [ ] **T1.3** 封装 ifind_announcement（公告标题 + PDF URL，给"待核实事项"提供原始材料线索）
- [ ] **T1.4** 工具调用审计日志（每次调用记录：工具名/入参/耗时/授权结果）
- [ ] **T1.5** 注册进 deer-flow 工具体系，Agent 能自主调用 → 验收：问"寒武纪最新毛利率"，Agent 自己调工具答出且带四要素

### Phase 2：Agent 层场景化（5–9h）

- [ ] **T2.1** 改写 Supervisor/Planner prompt：意图识别 → 匹配技能卡 → 出研究计划
- [ ] **T2.2** 技能卡①「命题验证」：prompt 模板 + 工具白名单 + 产物格式（复用③的寒武纪证据链做演示案例）
- [ ] **T2.3** 技能卡②「业绩点评」（增长/盈利/费用/减值四段式模板）
- [ ] **T2.4** Reporter 产物模板：结论 + 证据清单（每条带来源/时点/单位/口径）+ **待核实事项** + 失效条件
- [ ] **T2.5** 冲突标记：同字段两源/两期不一致 → 产物中并排标出，不强行统一
- [ ] → 验收：跑通"验证'寒武纪盈利改善来自主营业务'"，产出完整报告

### Phase 3：审批与合规（9–13h）

- [ ] **T3.1** 计划审批 interrupt：计划卡可编辑，批准后才执行 → 验收：演示删掉一步再批准
- [ ] **T3.2** 输入拦截：涨跌预测/买卖建议 → 拒绝 + 引导改写（系统 prompt + 规则双保险）
- [ ] **T3.3** 输出核验：报告数字必须能回指工具返回值（程序渲染数字，LLM 只写解释）
- [ ] **T3.4** 页面固定 disclaimer："仅做事实研究，不构成投资建议"

### Phase 4：前端三栏（13–16h）

- [ ] **T4.1** 左栏：线程列表 + 新目标输入 + 能力页
- [ ] **T4.2** 中栏步骤流：计划卡 / 工具卡（可展开）/ 审批卡 / 事件标记（压缩、失败、恢复）
- [ ] **T4.3** 右栏产物面板：报告渲染 + 证据清单 + 导出 Markdown
- [ ] **T4.4** 底部成本条：token / 耗时 / 步数（数据来自 Langfuse）

### Phase 5：记忆、检查点、降级（16–18h）

- [ ] **T5.1** 检查点恢复演示路径：执行中刷新页面 → 断点续跑（录视频的杀手镜头）
- [ ] **T5.2** 长期记忆：Store 存标的与历史结论 → 验收：新会话追问"上次那家公司研发费用率结论"
- [ ] **T5.3** 上下文压缩事件上报 UI（压缩处打标记，原文仍可查）
- [ ] **T5.4** 降级链路：拔 iFinD → 步骤标红"数据源不可用"任务继续；LLM 失败重试一次 → 验收：断源演示可复现

### Phase 6：收尾（18–24h）

- [ ] **T6.1** 测试说明：主链路 T1–T4 / 异常 T5–T8 / 合规 T9–T12（见第 4 节），逐条跑并记录
- [ ] **T6.2** README：启动方式、环境变量、产品选择（含三家对标）、AI 角色、数据来源、已知边界、未做事项
- [ ] **T6.3** AI 使用与验证记录：随手记的 3–5 条真实修正（含"用 Kimi 工具链复刻迷你版 Kimi 金融方案"这句）
- [ ] **T6.4** 部署公网 URL（前端 Vercel + 后端一台容器机，SQLite 即可，别上 Postgres）
- [ ] **T6.5** 录 60–180s 演示视频（题目标可选，但检查点恢复/审批只有视频能充分展示）→ 一镜到底：输入目标 → 改计划 → 批准 → 刷新恢复 → 出报告 → 点数字溯源
- [ ] **T6.6** 最终核对交付清单（第 5 节）

### P1（有余力才做，严格按顺序）

- [ ] **P1.1** 技能卡③「持仓早报」+ cron 定时触发（演示事件驱动 + 记忆）
- [ ] **P1.2** ifind_news 工具
- [ ] **P1.3** 报告数字点击弹出原始工具返回 JSON（追溯到底）

---

## 4. 测试用例清单

**主链路**：T1 完整跑命题验证；T2 编辑计划再批准（审批非摆设）；T3 执行中刷新断点续跑；T4 新会话验证长期记忆。
**接口异常/数据缺失**：T5 拔 iFinD → 标红继续；T6 不存在的股票代码 → 显式报错；T7 字段为空 → 报告注明"未披露"；T8 LLM 失败 → 重试 + 保留检查点。
**极端/合规**：T9 "下周会涨吗" → 拒绝预测；T10 "该不该买" → 引导改写；T11 模糊目标触发步数/预算上限显式停止；T12 每个数字可回指工具返回，四要素齐全。

## 5. 交付清单核对

- [ ] 可访问可操作的 Web URL
- [ ] 源码仓库 + README（启动/环境变量/产品选择/AI 角色/数据/边界/未做事项）
- [ ] AI 使用与验证记录（3–5 条真实修正）
- [ ] 测试说明（三类场景执行记录）
- [ ] 演示视频（强烈建议交）
- [ ] 仓库无 Key、无隐私数据
- [ ] 无涨跌预测/收益承诺/买卖建议；事实/推断/不确定有区分

---

## 6. 风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| iFinD MCP 登录态失效/调不通 | 高 | Phase 0 就验证；全程把工具返回存 fixture，演示可回放 |
| deer-flow 耦合太重改不动 | 中 | 备选：LangGraph + agent-chat-ui 自拼（+3~4h），Phase 0 内做决断 |
| 前端三栏耗时超支 | 中 | 先保中栏步骤流（评分核心），左右栏可简陋 |
| Agent 跑太慢/太贵 | 中 | token 预算卡死；演示任务预跑一遍拿 fixture 兜底 |
| 时间不够 | 中 | 按 P1 清单砍，P0 一个不许砍 |
